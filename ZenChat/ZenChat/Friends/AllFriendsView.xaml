<Page
	x:Class="ZenChat.Friends.AllFriendsView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:ZenChat.Friends"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d" d:DataContext="{d:DesignInstance local:AllFriendsViewModel}">
<Viewbox>
	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="10*" />
			<RowDefinition Height="1*" />
			<RowDefinition Height="0.7*" />
		</Grid.RowDefinitions>
		<ListView ItemsSource="{Binding MyFriends}" Grid.RowSpan="1" Grid.Row="2">
			<ListView.ItemTemplate>
				<DataTemplate>
					<Canvas>
						<TextBlock Text="{Binding Name}" />
					</Canvas>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
		<TextBox Grid.Row="1" Width="200" Height="30"
		         Text="{Binding NewFriendPhoneNumber, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
		         Margin="113,11,47,11.667" />
		<Button Grid.Row="1" Command="{Binding AddFriendCommand}" Content="ADD Friend" />
	</Grid>
</Viewbox>
</Page>